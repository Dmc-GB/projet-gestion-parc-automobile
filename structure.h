

void creer_parc_voiture(Voitur *voiture)
{
        strcpy(voiture->modele,"TOYOTA");
        strcpy(voiture->matricule,"RB-AT100");
        voiture->kilometrage=140;
        strcpy(voiture->etat,"Disponible");

        strcpy(voiture->modele,"NISSAN 2015");
        strcpy(voiture->matricule,"RB-AA200");
        voiture->kilometrage=156;
        strcpy(voiture->etat,"Disponible");

        strcpy(voiture->modele,"LAMBORGUINI NEW");
        strcpy(voiture->matricule,"RB-DD300");
        voiture->kilometrage=290;
        strcpy(voiture->etat,"En cours de location");

        strcpy(voiture->modele,"HONDA 115");
        strcpy(voiture->matricule,"RB-AA123");
        voiture->kilometrage=270;
        strcpy(voiture->etat,"Disponible");

        strcpy(voiture->modele,"TOYOTA 4X4");
        strcpy(voiture->matricule,"RB-RE125");
        voiture->kilometrage=240;
        strcpy(voiture->etat,"En cours de location");

        strcpy(voiture->modele,"MERCEDECE BENZ");
        strcpy(voiture->matricule,"RB-MB300");
        voiture->kilometrage=295;
        strcpy(voiture->etat,"En cours de location");

        strcpy(voiture->modele,"COROLA 2019");
        strcpy(voiture->matricule,"RB-CA200");
        voiture->kilometrage=300;
        strcpy(voiture->etat,"Disponible");

        strcpy(voiture->modele,"VENZA 110");
        strcpy(voiture->matricule,"RB-VZ200");
        voiture->kilometrage=310;
        strcpy(voiture->etat,"En cours de location");

        strcpy(voiture->modele,"MAZDA 100");
        strcpy(voiture->matricule,"RB-MZ150");
        voiture->kilometrage=280;
        strcpy(voiture->etat,"Disponible");

        strcpy(voiture->modele,"PRADO 450");
        strcpy(voiture->matricule,"RB");
        voiture->kilometrage=330;
        strcpy(voiture->etat,"Disponible");

}









